# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next
out
build
dist

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log

# Scratch (dev artifacts, per CLAUDE.md)
tmp

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Generated content renders — produced by `pnpm content:mermaid`, keyed by a
# hash of their source, so reformatting them would only churn the diff.
apps/*/public/generated

# GitHub item exports — `scripts/export-github-item.py` renders a thread
# verbatim, and the vet run reads the tree before `/finalize` sweeps these, so
# without this every branch carrying one fails `format:check` on a file whose
# formatting is the exporter's to decide.
docs/issue
docs/pr

# Lock files
pnpm-lock.yaml
package-lock.json
yarn.lock
