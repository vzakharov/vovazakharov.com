# Organisation marks

Each file is the organisation's own favicon or app icon, **trimmed to its ink
box and re-padded to a centred transparent square**. Vendors inset and offset
them however they like, and the cards render them all at one size with no
per-logo nudges — so an untrimmed file lands visibly smaller and off-axis
beside its neighbours. Trim a new one before committing it.
