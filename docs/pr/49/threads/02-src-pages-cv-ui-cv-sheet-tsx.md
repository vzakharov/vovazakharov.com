# `src/pages/cv/ui/cv-sheet.tsx`

<a id="t02"></a>

### `src/pages/cv/ui/cv-sheet.tsx`:78 — unresolved

```diff
@@ -69,9 +70,12 @@ export function CvSheet({ variant, caseStudyHref }: CvSheetProps) {
           <Box component="header" className={classes['header']}>
             <Stack ta="center" className={classes['section']}>
               <Title order={1}>
-                <InternalLink href="/" underline="never" inherit>
+                {/* Absolute because the header prints: a relative href resolves
+                    against whatever host rendered the PDF. The screen cost is a
+                    full navigation, which the footer's back-link already covers. */}
+                <Anchor href={printedUrl('/').href} underline="never" inherit>
                   {t('header.name')}
-                </InternalLink>
+                </Anchor>
```

**@vzakharov (human)** — 2026-09-16T08:14:13Z

аналогично. вообще хочется понять разницу Anchor и InternalLink с точки зрения механики рутинга

**@vzakharov (agent)** — 2026-09-16T09:06:48Z

Здесь — да, и это была настоящая регрессия. Поправил в 1482060.

**Что было.** Я заменил `InternalLink href="/"` на абсолютный `Anchor` сразу для обоих носителей и обосновал это в плане: расщепление на две ссылки «держит имя в DOM дважды ради одного мягкого перехода, не стоит того». Цену я при этом ни разу не измерил, а она нулевая — вторая ссылка под `print-hidden`, и после расщепления все четыре PDF пересобрались байт в байт (`kept … — the render is unchanged` ×4). Сам issue #47, кстати, просил трогать только печатный путь. Теперь в заголовке ровно та же пара, что и в `case-study-link.tsx`: экранный `InternalLink` с относительным href, печатный `Anchor` с абсолютным.

**Про механику.** `InternalLink` — это буквально `<Anchor component={Link}>`, где `Link` — это `next/link` (`src/shared/ui/internal-link.tsx`). Разница в том, кто обрабатывает клик:

- `Anchor` — голый `<a href>`. Клик обрабатывает браузер: новый запрос документа, полная перезагрузка, React монтируется с нуля, клиентское состояние теряется.
- `InternalLink` — такой же `<a href>`, но `next/link` вешает на него обработчик: перехватывает клик, подтягивает RSC-пейлоад маршрута и подменяет дерево на месте, без перезагрузки документа. Плюс префетч при наведении и при попадании во вьюпорт.

Адрес в обоих случаях один и тот же — у нас статический экспорт, никаких рерайтов. Отличается только переход.

И вот здесь две темы сходятся в одну: `next/link` пишет в HTML **относительный** href (`href="/"`). На экране это ровно то, что нужно. В PDF относительный href резолвится против хоста, который печатал файл, то есть против `next dev` — отсюда и `http://localhost:41593/` в issue. «Почему `InternalLink`» и «почему PDF сломан» — это один и тот же факт с двух сторон.

Отдельно: на той же правке вылез `vova/no-redundant-property-copy` на `href={printed.href}` в обоих файлах — он тоже в 1482060, через деструктуризацию. Вет я ещё не гонял (это работа `/finalize`), так что до сих пор ветка его бы не прошла.

---

<a id="t04"></a>

### `src/pages/cv/ui/cv-sheet.tsx`:93 — resolved

```diff
@@ -70,10 +72,23 @@ export function CvSheet({ variant, caseStudyHref }: CvSheetProps) {
… 4 lines elided …
-                    against whatever host rendered the PDF. The screen cost is a
-                    full navigation, which the footer's back-link already covers. */}
-                <Anchor href={printedUrl('/').href} underline="never" inherit>
+                {/* One anchor per medium: a client-side route needs the relative
+                    href `next/link` renders, and a relative href in a PDF
+                    resolves against whatever host printed the file. */}
+                <InternalLink
+                  href="/"
+                  className="print-hidden"
+                  underline="never"
+                  inherit
+                >
+                  {t('header.name')}
+                </InternalLink>
+                <Anchor
+                  href={PRINTED_HOME}
+                  className={classes['printLink']}
+                  underline="never"
+                  inherit
+                >
                   {t('header.name')}
                 </Anchor>
```

**@vzakharov (human)** — 2026-09-16T09:14:48Z

не-не-не, WET и вообще некрасиво. Я так понимаю у нас "для принта делай так, для экрана" эдак -- стандартная вещь, поэтому может стоит сделать единый компонент с вилкой и использовать его везде?

**@vzakharov (agent)** — 2026-09-16T09:46:04Z

Согласен, и это второй раз за один PR, когда план оказался неправ в этом месте — причём в противоположную сторону. Сделано в e7f4b35.

Смешнее всего то, что аргумент против компонента я написал сам, в `## DRY notes`: «два колл-сайта хотят от хелпера разного, обёртка на оба возьмёт флаг „мой текст или хелперский“ — это те же два места, только с булевым между ними». Флаг действительно есть (`withAddress`), так что формально я был прав. Но это одно слово на колл-сайте, а взамен уходит вся вилка из обоих мест:

```tsx
<PrintableLink {...{ href }} withAddress inherit>
  {label}
</PrintableLink>
```

`case-study-link.tsx` при этом похудел с 39 строк до 26, а в `cv-sheet.tsx` заголовок стал тремя строками вместо шестнадцати.

**Что внутри.** `PrintableLink` (`src/shared/ui/printable-link.tsx`) рендерит обе половины: `print-hidden` `InternalLink` на относительном href — тот самый клиентский роутинг из соседнего треда — и печатный `Anchor` на `printedUrl(href)`. `withAddress` отвечает ровно на один вопрос: выписывает ли бумага адрес после текста. В строке про кейс-стади — да («Playgram case study: vovazakharov.com/...»), у имени в шапке — нет, потому что адрес сайта и так напечатан строкой ниже.

Класс `printLink` уехал туда же, в `printable-link.module.scss`. Это, пожалуй, главное: пока он лежал в `cv.module.scss`, вторым потребителем паттерна мог быть только CV.

**Где я проверил, что «везде» — это именно эти два места.** Остальные `print-hidden` / `print-only` в дереве (тулбар, футеры, TOC, видео-заглушка в `rehype-media-embeds`) — это одностороннее скрытие, один элемент на один носитель. Там нет вилки, которую можно вынести: класс и так делает всю работу, компонент был бы обёрткой над именем класса.

**Побочно — и это важнее, чем сам рефактор.** Перерендер после правки показал, что два PDF кейс-стади меняются, хотя их DOM не менялся. Оказалось: Chromium нумерует узлы structure tree (accessibility) от счётчика, который не устаканивается между запусками — одна и та же неизменная страница печатается то с `node00000140`, то с `node00000141`, а на первом запросе к свежеподнятому dev-серверу вообще с `node00000339`. То есть защита от churn, ради которой этот PR наполовину и существует, покрывала только CV — у них structure tree нет — и честно переписывала оба кейс-стади из-за байтов, до которых не дотягивается ни читатель, ни ссылка.

Почини́л в 7d985ac. Сравнение теперь переименовывает каждый узел по порядку первого появления: счётчик пропадает, а все алиасы сохраняются — ячейка, которая ссылается на другой хедер, чем раньше, по-прежнему читается как изменённый документ. Вариант «прогревать роут перед печатью» я проверил и выбросил: два прогретых рендера всё равно расходятся, счётчик именно неустойчивый, а не холодный.

`sameRender` заодно переехал в `scripts/lib/same-render.ts` — сам скрипт рендерит на импорте, так что оттестировать функцию на месте было нельзя. Пять кейсов: что он обязан простить (часы, другая база узлов) и что не имеет права (изменившийся алиас, сдвинутый контент, сменившийся `/URI`). Полный прогон после этого — `kept` на всех семи файлах, в `git status` только манифесты.

Вет целиком (`pnpm build` обоих сайтов, typecheck, eslint, stylelint, steiger, type-overlap, test, оба `--check`) зелёный, но это всё ещё не `/finalize` — мерж main и флип в ready ждут отдельной команды.

**@vzakharov (human)** — 2026-09-16T11:31:37Z

тут у меня вопрос по имени. Ты завёл новый компонент (PrintableLink), а бывают ли у нас другие link вообще? то есть распечатать-то мы можем каждую страницу, и в каждой ожидаем, что и на экране и на печати ссылки будут легитимные.

Если это правильное понимание, то тут либо

1- линтер должен запрещать использовать другие ссылки в компонентах которые могут идти со скачивалкой `.pdf` (но тогда тот же вопрос, как отличать такие компоненты)

2- либо _любые_ ссылки должны знать, что их могут распечатать.

Если я неправильно понял, поясни пжст, желательно с примером.

**@vzakharov (agent)** — 2026-09-16T11:47:27Z

Понял правильно, и это вариант 2. Сделано в 6c02db8: вилка переехала внутрь `InternalLink`, `PrintableLink` больше нет.

**Где я был неправ в имени.** `PrintableLink` рядом с `InternalLink` читается так, будто печатность — свойство, которое ссылка себе выбирает. Такого свойства нет. Есть ровно один факт: `next/link` пишет в HTML **относительный** href. В браузере это то, что нужно, а в PDF он резолвится против хоста, который печатал файл. Значит вилка нужна каждой ссылке, которую рисует `next/link`, и никакой другой.

**Но не «любым ссылкам» — и вот тут поправка к твоей формулировке.** Внешние ссылки уже абсолютные по построению, чинить в них нечего:

| что в дереве                                           | href в HTML                          | в PDF                     |
| ------------------------------------------------------ | ------------------------------------ | ------------------------- |
| `InternalLink` (`next/link`)                           | `/cv/cto/en`                         | ломался — **теперь пара** |
| `<Anchor href="https://…">`, `mailto:`                 | абсолютный                           | всегда был цел            |
| `printedUrl()` — `printed-from`, `rehype-media-embeds` | абсолютный                           | всегда был цел            |
| `CardLink`                                             | внешний URL из контента              | всегда был цел            |
| TOC (`<a href="#id">`)                                 | только фрагмент, плюс `print-hidden` | нечего ломать             |

Это не теория — это видно в уже закоммиченных PDF кейс-стади, которые никогда не были сломаны:

```
$ strings apps/vova/public/case-studies/playgram.pdf | grep -o '/URI ([^)]*)' | sort -u
/URI (https://bubble.io/blog/ohana/)
…
/URI (https://vovazakharov.com/case-studies/playgram)
```

Ни одного `localhost` — при том что на этой странице печатается футер со своим адресом на каждой полосе. Потому что он идёт через `printedUrl()`, а единственный `InternalLink` на странице («← Case studies») лежит в `print-hidden`-навигации. Ломались ровно две ссылки в CV, и обе — `next/link`.

**Про вариант 1 (линтер).** Запрещать нечего: все прочие ссылки и так корректны. Единственное правило, которое здесь что-то стоит, — «`next/link` импортируется только в `internal-link.tsx`», и оно уже выполняется де-факто (`grep -rn "from 'next/link'" src/` даёт одну строку — сам `internal-link.tsx`). Могу дописать `no-restricted-imports` с исключением на этот файл, если хочешь превратить факт в проверку; сам не стал, чтобы не расширять PR про PDF.

**Пример, как просил.** Колл-сайт не изменился ни на символ, кроме имени:

```tsx
<InternalLink {...{ href }} withAddress inherit>
  {label}
</InternalLink>
```

На экране: `<a href="/case-studies/playgram">Playgram case study</a>`, клик перехватывает `next/link`.
На бумаге: `Playgram case study: vovazakharov.com/case-studies/playgram`, аннотация ведёт на `https://vovazakharov.com/case-studies/playgram`.

**Одно исключение — `InternalButton`**, и оно теперь написано в коде: кнопка — это то, что нажимают, а бумага нажатий не принимает. Раньше она печаталась рамкой с мёртвым относительным href; теперь `print-hidden`. Один колл-сайт (`read-cv-button` на главной), PDF у главной нет, так что байты никуда не сдвинулись.

**Чем проверено, что расширение безопасно.** Теперь печатную половину получают все `InternalLink` в дереве — их в `src/` десяток. Полный перерендер: `kept` на всех семи PDF, в `git status` только манифесты. То есть ни одна печатная полоса не сдвинулась: каждый оставшийся `InternalLink` лежит внутри `print-hidden`-контейнера (тулбар, футеры, навигация), и его печатная половина прячется вместе с ним.

Вет целиком зелёный.

---
