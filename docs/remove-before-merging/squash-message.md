Proposed squash title/body:

```
feat: the CV as pre-rendered markdown (pr #70)
```

```
The CV's copy lived as a 277-line slice of the message catalogues, where
a paragraph was a JSON string and a bullet a `{label, text}` record — a
second content system beside the pipeline that already compiles the case
studies and the Bible. It goes through that pipeline now: four markdown
documents under `apps/vova/public/cv/`, compiled at build time, and
served raw at each route plus `.md` beside the PDF that used to be the
only file a reader could take away.

Variant is a directory and locale a file — `cv/cto/en.md`, beside the
`cv/cto/en.pdf` that route already served — so the CV keeps the
file-at-its-route rule without touching the dotted-suffix collision
between a document's cut and the unbuilt locale seam. Each of the four
states its framing in full: the variant merge that gave every shared
sentence one home per language is gone, and with it the guarantee that a
key `cto` left unstated could not drift from `dev`.

The sheet looks as it did, on screen and on paper. Its body compiles to
HTML and is injected as an article's is, so `cv.module.scss` becomes a
structural stylesheet beside `prose.scss` — a CSS module's hashed names
being unreachable from injected markup — and one `:::entries` directive
marks the section whose cards are per-entry. The social-card generator
and the PDF source hashes read the markdown where they read the
catalogue, and all four PDFs and both cards are re-rendered here.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
