Proposed squash title/body:

```
docs: bring the README up to the two-site layout (pr #48)
```

```
The two-site split moved every router, public/ and Next config under
apps/<site>/, and the README went on describing the tree that preceded
it: a root app/, a root public/, and a pages/ shadow whose own README
was deleted with it. Its Development block handed out `pnpm dev`, which
the split removed -- the first command a new reader runs.

Structure, deployment and routes are rewritten against what ships: both
sites and the different doors they leave by, the feat:/fix: gate that
decides whether a merge publishes at all, and the routes each site
actually serves, /dev and /contact having been listed long after they
stopped existing. The dead script was quoted once more in
render-pdf.ts's dev-server diagnostic, where it is the one thing a
reader would run next.

Where the README restated a convention CLAUDE.md owns -- the vet suite's
contents, the deploy gate's edge cases, a one-time Pages setup that
belongs to /stand-up-site -- it now points instead, so each convention
keeps a single home and a change to one lands in one place.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
