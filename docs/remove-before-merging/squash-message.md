Proposed squash title/body:

```
chore: sync the agent boilerplate forward to 4998149 (pr #34)
```

```
The boilerplate this repo vendors its agent infrastructure from kept
editing it, and two commits had landed there since the last sync. Both
apply here: every skill they touch is adopted, and neither is bound to
the source's stack.

`scripts/check-squash-message.sh` settles the part of the squash-message
target a machine can. `/squash-message` states it as prose — a
one-line title, a body hard-wrapped at ~72 — and its tighten pass is
an agent reading its own output; the script measures the proposal's two
fenced blocks against 80 chars of title and 50 lines of body at 72 wide,
exempting a line whose trimmed text is a single unwrappable token. It
joins the vet fan-out as its tenth check, reading only the proposal, so
it catches what authorship cannot: a proposal edited by hand, or
outgrown by a later base merge. Because `/finalize` sweeps
`docs/remove-before-merging/` first, it walks a ladder to find one —
worktree, `tmp/`, staged sweep, then the commit that deleted it, bounded
to the merge base so another branch's proposal is never measured as this
one's.

The rest gives each skill in the loop one thing to own. `/plan` now
publishes its own plan as a draft PR, so a plan is reviewed as a diff,
with inline comments and threads, instead of chat prose; the trigger
sits in `/plan` because that is where a plan becomes pushed.
That leaves `/pr` with no reason to take a task argument: it owns the PR
object in three modes, the old duplicate-PR guard inverted into a
refresh that re-derives body and QA checklist against the diff the plan
could only forecast. `/implement` becomes `/go`, keeping a redirect stub
for the handoff blocks in open PR comments that still say the old name.
One predicate carries the review loop — implementation has begun iff
`docs/plans/` holds a file that is not a draft — so a draft plan plus
unanswered feedback reads as review of the plan, and no review comment
unlocks source edits; only an invocation does.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
