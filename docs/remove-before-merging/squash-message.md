Proposed squash title/body:

```
chore: sync the agent boilerplate forward to 71c6201 (pr #30)
```

```
The boilerplate advanced one squash commit since 7f3ca76, carrying six
independent changes to the agent infrastructure this repo vendors from
it.

The GitHub exporter splits into `scripts/gh_export/`, one module per
concern behind a 155-line entrypoint, and a PR export now states each
review thread's resolved, unresolved or resolution-unknown state.
Resolution is the one fact no REST comment payload carries, so it comes
from GraphQL, keyed by every comment id in a thread rather than the
root: the export rebuilds threads from REST `in_reply_to_id` chains,
which root on a different comment when a parent falls off the page.

The plan lifecycle gains `*.paused.md`. `*.in-progress.md` meant both "a
session holds this now" and "resume here", so two sessions could claim
one plan and overwrite each other's commits; it is a claim only now,
released as `*.paused.md` by a session told to stop partway, and
`/implement` counts only the actionable suffixes instead of stopping on
every sibling. `/tighten-docs` gains an existence lens ahead of its two
— whether the prose should exist at all — carrying the ordered homes and
the rule-vs-README test, plus a single-lens mode; CLAUDE.md's doc-sync
section becomes "Writing things down", keeping the threshold and naming
the skill as the long version, since a rule loads in full every time its
glob matches.

`/handle` is new: attach to a branch, read off whether it holds an
unimplemented plan or unanswered review feedback, and run that lane,
with land-prep opt-in. It is this sync's one offered-not-taken
decision, taken because every skill it composes is already hydrated
here and it references nothing declined. Two conventions ride along —
reads and edits stay on Read/Edit/Write in every permission mode, and a
colour change needs both themes captured — and the watermark advances to
source HEAD rather than the last commit taken, recording
`docs/catalog.md` as declined so it stops surfacing as an open question.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_014S1eDB5SepBgbvaMA1Guu4
```

---

_Generated by [Claude Code](https://claude.ai/code)_
