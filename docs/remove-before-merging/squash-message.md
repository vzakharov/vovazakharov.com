Proposed squash title/body:

```
feat: Playgram CV entry and case study page (pr #3)
```

```
Five months of work on a private repository had no public record. This
adds both artifacts the plan called for: a new CV experience entry, and
a case study at /case/playgram telling what was built and why, how it
was built, and the interesting trouble. Both are spined on the rebuild —
a replacement that had to reach parity with a live, still-shipping
Bubble.io product and then take over from it is a sharper claim than
"built an AI app", and the cutover completed.

Adding a CV entry is a three-file change, because the experience section
is hardcoded JSX rather than data-driven: a cv.experience.playgram
object in both message catalogs, whose keys and array lengths must match
or the build breaks, plus a Card reading it. It goes above project1 to
keep the section reverse-chronological; project1 is a different
engagement and is untouched. The case study is English-only, so it sits
outside app/[locale]/ and follows the app/page.tsx shape — a thin
page.tsx holding constructMetadata() and re-exporting the content.
Homepage and CV both link to it, print-hidden on the CV in favour of a
bare URL so the PDF export carries no dead text.

Every published figure was recomputed against a fresh clone rather than
carried over from the plan, whose numbers predated three more commits,
and the page stamps the commit it was verified against because the
project is suspended rather than finished. That pass corrected five
claims: 28 project-local lint rules rather than 27, and only 26 of them
carrying tests; a 64-commit weekly median peaking at 107; four
sweep breaches across two filenames rather than four files; ~132k lines
of application code against ~120k of tests; and the other engineers'
share of net lines risen to 13%.

Three things are deliberately absent, each pending a call that is not
the implementer's: the three teammates' names, which need their own
consent even though the founder cleared the project; migration 0112 as a
quotation, since it embeds real customer emails and workspace names; and
the week-2 self-assessment tension, which reads as candour in context
and as damage out of it.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
