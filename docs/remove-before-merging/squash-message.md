Proposed squash title/body:

```
feat: an opaque chart, a rasterized OG card, and richer Playgram cuts (pr #20)
```

```
The units-of-work chart is an <img>, and an SVG inside <img> resolves
prefers-color-scheme against the OS rather than the page — so the site
in dark mode on an OS set to light was already painting #0b0b0b text
onto #000000, and a paste into a dark document did the same. The chart
now paints a full-bleed rect first, filled from the same media query as
its palette and carrying globals.css's own --background values, so it is
self-consistent wherever it lands.

That chart also becomes the Open Graph card for all three cuts, which
takes more than a frontmatter line: no major consumer renders an SVG
og:image, and a static export has no request-time renderer. So
`pnpm content:og` rasterizes it into a committed 2400x1260 PNG, and
`--check` — in the vet run — fails when the source's hash no longer
matches `og-renders.json`. The card's dimensions are read from the PNG
and published, replacing constructMetadata's assumption that a custom
image's size is unknowable. Two headless-Chromium traps are pinned down
in comments there: an SVG spliced into HTML breaks out of foreign
content on any of some forty tag names, and a rasterized SVG image
loses its bottom rows when its box ends on the surface's last one.

The mini and nano cuts gain the chart and six other assets that already
existed in the full document, so neither reads as a wall of text. The
copied mermaid fence is byte-identical, which is what lets both
documents share one committed render pair.

`micro` is renamed `nano` so the two cuts' leading letters differ;
VARIANTS is the source of truth, so the routes, the sitemap and the cut
switcher all follow, and the old /micro route 404s because a static
export on GitHub Pages cannot redirect. Three helpers that gained a
second caller move to one home each — the image-header reader to
shared/content/image-dimensions.ts, mermaidHash to contentHash, and
findChromium plus the content-tree walk to scripts/lib/.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01SLiHWoiJ543phidZZFzQMM
```

---

_Generated by [Claude Code](https://claude.ai/code)_
