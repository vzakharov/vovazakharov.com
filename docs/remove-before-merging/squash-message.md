Proposed squash title/body:

```
feat: adopt the playgramapp linting ruleset (pr #16)
```

```
The lint config was thirty lines of Next presets with Prettier
wired both inside ESLint and beside it, leaving the type-aware
@typescript-eslint rules, the full jsx-a11y set and every
React-19 rule unenabled. At this size the explicit ruleset
playgramai/playgramapp runs is one pass rather than a migration,
so it lands whole.

eslint.config.ts, loaded via jiti, orchestrates the preset
spreads and plugin registration; the rules live in
eslint/rule-groups/*.ts by plugin family, every one error or off
— never warn — each off carrying its rationale and, where it
delegates to the compiler, one of the twelve strictness flags
tsconfig.json gains here. Nine of upstream's thirty
project-local rules port under a vova/ namespace, the rest
assuming a database, server actions, Mantine or Zod a static
export does not have. eslint-plugin-prettier goes, since
format:check was always the check doing the work.

What it surfaced was mostly real: an English aria-label on the
Russian CV, {stars && …} rendering a bare 0, three untitled
Spotify embeds. next-intl.d.ts registers the message catalogs
with next-intl, so useTranslations keys are checked against
en.json and useMessages() returns the catalog's real shape —
which also retired eighteen no-unsafe-* reports by replacing
t.raw(). @steiger/toolkit becomes a devDependency because the
steiger plugin's shipped declarations import it, and without it
every export it types reads as any.

Exemptions sit at the point of use — an inline disable carrying
its rationale, where whoever edits the line will read it —
rather than soften a rule to warn; a config block is for an
exemption spanning files, which leaves one, for eslint/ itself.
Where a rule's premise is simply wider than the API, the rule
learns the API instead: no-floating-promises knows node:test's
describe and it, whose promise the runner awaits.
.claude/rules/eslint.md records that and the error-or-off
policy.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
