Proposed squash title/body:

```
chore: a PR export per turn, and the entry ladder at the prompt (pr #54)
```

```
Two ends of the agent loop answered from stale state. `/handle` read
whatever PR export an earlier turn had left in the tree, so it could
say "nothing changed" off an hour-old snapshot; and the ladder that
routes a session's opening prompt sat resident in CLAUDE.md, where it
was routinely skipped, so a change asked for in prose got made
directly — no plan-or-not call, no quality passes, no PR.

A `UserPromptSubmit` hook now takes the PR export before the turn
starts, and `/handle` commits what it read, so the next export arrives
as a `git diff` against it: an empty diff is the answer to "has
anything changed?", not the absence of one, and the committed sequence
shows a reviewer which state of the PR each turn was answering. A
second hook puts the routing call in front of the first prompt,
pointing at the ladder rather than copying its rows. Both share one
`first_prompt` guard, and `/finalize` sweeps the export tree before
the PR goes ready so the adds and the delete cancel out here.

Three skill sections that fire on a single invocation form move to
colocated pages, paid for only when their trigger comes up — native
plan-mode recovery, the working-file pick `/squash-message` needs on a
first run, and frame extraction for a video attachment. The catalog
check gains the reverse of its reference assertion, a page nothing
points at failing as silently as a pointer to nothing. An export is
now one file however long the PR, the hoist into `threads/` having
turned one arriving comment into a ten-file commit. And `/pr`
refreshes over REST: `gh pr edit` asks for project cards on every
edit, so where classic projects are deprecated it fails and leaves the
PR exactly as it was.

The repo we vendor this loop from moved; the watermark records how far.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
