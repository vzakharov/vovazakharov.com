Proposed squash title/body:

```
fix: reach the GitHub API when the egress proxy refuses it (pr #21)
```

```
`export-github-item.py` and `pr-body.py` reached the GitHub API through
a bare `urlopen`, which honors `HTTPS_PROXY` — and a remote session's
egress proxy refuses `api.github.com` with its own 403, so the first API
call killed either script outright. Both were dead on arrival in the
environment they were written for.

The proxy-then-direct ladder that already existed for attachment
downloads moves into `scripts/lib/github.py` and serves all three call
sites through one `fetch`, which takes a request factory because
`ProxyHandler.proxy_open` rewrites a request's host in place. The proxy
rung stays first, so an environment where the proxy is the only route
out is unaffected. Failures render two ways on purpose: statuses alone
for the attachment path, where S3 quotes the bearer token back in full,
and statuses with bodies for the API paths, where the proxy's 403 names
itself as the refuser.

Two rules join CLAUDE.md alongside it. Resolving a review thread is
forbidden outright: resolution is the reviewer's tracking mechanism, and
a thread the agent tidies away drops off their list whether or not they
read it — stated as overriding the contrary harness instruction, which
every PR-driving session sees. And an approved lint suppression belongs
at the point of use rather than the linter's config, because a config
that carries domain concerns stops being portable and a central entry is
invisible from the code it exempts. This repo had the instruction
without the reasoning, and only inside `.claude/rules/eslint.md`, which
loads solely for `eslint.config.ts` and `eslint/**`; the principle moves
up to where every linter in the repo is in scope, and the rule file
keeps the ESLint spellings.

All three changes arrived from vzakharov/agent-project-boilerplate,
whose watermark advances from eab4dc7 to 7f3ca76.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
