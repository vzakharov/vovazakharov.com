Proposed squash title/body:

```
feat: carry the CTO positioning to the landing page and card (pr #35)
```

```
PR #31 repositioned the CV around a fractional, hands-on CTO offer and
left behind the two surfaces that carry it to strangers. External
feedback from an industry CEO on the live site named the first: the
landing page still opened as a developer looking for a position, under
a section heading that read `/dev`. Chasing its last point — that the
picture suits a developer and not a CTO — turned up the second. The
CV's social card was a hand-committed PNG with the dev tagline baked
into the pixels, served as `og:image` for both framings, so a shared
CTO link unfurled beside a card that said DEVELOPER.

The landing page now makes the offer in its first screen and splits in
two: `/offer` states what the engagement is and names each kind of it,
`/work` keeps the projects and highlights that evidence it. The hero
tagline and the engagement names are read out of the CV's own message
catalogue rather than retyped, so the two pages cannot come to disagree
about what is on offer — that tagline had been spelled two ways once
already. The personal line the page used to open with moves down beside
the footer's see-also links.

The offer itself gains the engagement the feedback asked for:
architecture drawn and the foundation stood up for a team that writes
the features itself, which is a different purchase from delivery and
from the agent pipeline. Standing technical judgment gives up the
clause the new bullet now covers as work rather than as advice.

The CV keeps both framings and stops offering them as a coin flip: the
header's two-button toggle becomes one directional line at the foot of
the sheet, so each framing still has an address to share and neither
reads as an unmade decision. The stale card goes with it, leaving the
portrait every other page already falls back to — published, at last,
at the size it actually is rather than the 150 square the config
claimed.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
