Proposed squash title/body:

```
perf: #74 ship each article body once, not twice (pr #81)
```

```
Every article page carried its body twice: once as the markup and once
in the RSC flight data App Router inlines for hydration. GitHub Pages
serves gzip only, and gzip's window cannot reach back to the first
copy, so the duplicate cost about half of what the page transferred.

The exported page now holds an empty dangerouslySetInnerHTML slot where
the body goes, which React leaves alone during hydration, and a
post-build step renders each body and splices it into its page. It
fails on a page whose slot is missing, names another document or stays
empty. The dev server still renders the body inline, so previews and
the PDF pipeline are unchanged. playgram.html drops from 68.3 kB to
35.8 kB gzipped.

Client-side navigation would render the empty slot, so InternalLink
turns any link to a document route into a plain anchor, and articles
load as whole pages.

Closes #74

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
