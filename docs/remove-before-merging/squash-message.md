Proposed squash title/body:

```
docs: read a PR's conversation from the export, not gh JSON (pr #58)
```

```
`/handle` picks its lane off whether the PR carries feedback nobody has
answered, and that read needs the thread structure, the resolved state,
and the `(agent)` / `(human)` labels that separate an agent's own reply
from an operator's guidance. Only the PR export carries them. Both steps
that run before it fetched the same conversation as `gh` JSON first,
where all three are flattened away — so an agent reached the export
already holding an answer, and had no reason left to open it.

The two fetches now ask only for what the export does not carry: state,
draft flag, base ref and merge state. The recency half of the lane test
takes its dates from the export and `git log`, and `/from-branch` states
once where a PR's body and threads come from, its deep-link bullet
pointing at that rather than repeating it.

`/from-branch` also reports, on attach, what the trunk has under
`.claude/` that the branch does not. Skills, rules and hooks are read
from the working tree, so a branch cut before a loop change runs the
loop as it stood then, and nothing about running it feels any different
— a rule the branch's copy lacks is one that session will not follow,
with nothing from outside to show the operator why.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
