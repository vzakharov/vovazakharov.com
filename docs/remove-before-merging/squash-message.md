Proposed squash title/body:

```
fix: the CTA button's box, lost to a stylesheet load order (pr #79)
```

```
Mantine's per-component stylesheets were imported alphabetically, and
that order decides what wins: a composite's root carries its base's
class alongside its own, both rules weigh one class, and both sit in
`@layer mantine`. `UnstyledButton` sorted last, after `Button`, so its
`padding: 0` and `border: 0` landed on top of what the button asked
for — leaving the home page's call to action a label with no box at
rest and a tight black block on hover.

`Text` after `Anchor` was the same collision one component over,
`text-decoration: none` over the theme's `underline: 'always'`. That
is the eight link underlines a5d18e5 read as the export's own
rendering and re-printed the CV PDFs to match; the four CV documents
are reprinted here with them back. The imports now follow the order
`@mantine/core/styles.layer.css` composes.

`check-mantine-styles.ts` measures that order as a third direction,
reading each built page's linked stylesheets in document order against
the aggregate's and printing the list to paste when they drift.
`render-pdf.ts` gains `theme-provider.tsx` as a print source: which
sheets ship, and in which order, shapes a printed page as surely as
the theme beside it, and without it a change there ships behind PDFs
the check calls fresh.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
