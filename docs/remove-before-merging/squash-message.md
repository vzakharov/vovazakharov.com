Proposed squash title/body:

```
chore: trim CLAUDE.md, stage always-loaded files, add a context budget (pr #89)
```

```
CLAUDE.md loads in full on every turn and had grown to ~61k characters,
most of it procedure only a session running one skill or touching one
path needs. It now states its own test: a line stays only when it must
hold on turns where its process is not running. It comes to ~32k: the
vet roster moves to `.claude/rules/stack.md`, the deployment mechanism
to `.claude/rules/deployment.md`, skill authoring to
`.claude/rules/skills.md`, and the rest into the skills and hooks that
run it. The rules README is path-scoped too, and check-skill-catalog.sh
now fails a section citation into CLAUDE.md that names no heading.

Always-loaded files are edited on a branch only through
`.claude/staged/<path>.staged` copies, which `/finalize` swaps in
(`scripts/staged.sh`), so an edit no longer invalidates the prompt cache
of every session on the branch. A PostToolUse hook warns at ~200k tokens
of context and pauses the work at ~300k unless it is nearly done.

The TypeScript cost ledger drops the session name, the one field the
agent wrote mid-turn into a tracked row, and commits each row by
plumbing so the tree never looks unfinished to the harness's Stop check.
Rows now record the operator's handle, the commit subject says what the
turn cost, and a row warns when its turn's tail was not yet written.
The rate table gains Opus 5.5, whose sessions wrote no row without it.

Routing now treats the first turn that asks for a change as a routing
point wherever it falls, not only a session's opening prompt. Syncs
vzakharov/muthur through 634bdfa.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
