Proposed squash title/body:

```
chore: PR export skips resolved threads, plus five skill fixes (pr #83)
```

```
The repo this one vendors its agent infrastructure from moved, and all
six of its changes apply here.

The PR export now drops threads the reviewer resolved, printing a count
of what it omitted; --include-resolved keeps them. /handle's tail test
never selected a resolved thread, so the export stops making an agent
read past them. CLAUDE.md now states that the loop's draft PR is a
defined step of /plan, /go and /pr, so the harness's "no PR unless
asked" default no longer turns it into an operator menu.

/handle stops ending on a note about the `and finalize` nobody typed.
/from-branch leads a failed attach with whether the session is on the
right repository. /override-gh says a GitHub tool refusal is a cue to
try gh rather than a dead end. /tend-prose ends its fix step with a
negator grep over the rewritten prose, and /plainly puts one minimal
concrete example after its causal chain.

Co-authored-by: Claude <noreply@anthropic.com>
```

---

_Generated by [Claude Code](https://claude.ai/code)_
