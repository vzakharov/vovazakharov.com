> ⛔ **DRAFT — DO NOT IMPLEMENT.** This plan is not approved. Do not edit source while this file is named `*.draft.do-not-implement.md` — prep and spikes go in `tmp/`. On an explicit operator go-ahead, `git mv` it to `*.in-progress.md` and delete this banner (quoting the go-ahead in the commit) _before_ touching code.

# A LinkedIn content plan mined from the Playgram case study

## The goal, stated plainly

A standing backlog of **small, self-contained LinkedIn posts**, each built on one concrete piece of work, mostly from the Playgram case study and later from whatever else ships. Every post has to leave a practitioner with something they can use on Monday. Nothing is a teaser for a longer thing; a post that only works as an advertisement is not a post.

Two effects are wanted and **neither is ever named in the text**: that a reader who needs this kind of work done starts wondering who does it, and that the name becomes one people recognise on the subject of running coding agents at scale. Both come from the writing being useful and specific, not from a closing line asking for anything. No CTA, no "what's your experience?", no "DM me". The dots stay for the reader to connect.

## What this change delivers

1. **`writing/linkedin/plan.md`** — the content plan: the post inventory, the recurring post shapes, the sequencing, and the form constraints.
2. **A PDF render of the case study** at `public/content/case-studies/playgram.pdf`, generated by `pnpm content:pdf` and committed, alongside the print stylesheet that makes it worth opening.
3. **`writing/linkedin/drafts/case-study-announcement.md`** — the calibration draft: the post that carries that PDF as an attachment.
4. **A `writing/` row in CLAUDE.md's repository-layout table**, and the `content:pdf` entries in the vetting list and `@.claude/rules/content.md`.

**Deliberately not in this change:** the anti-slop writing rules. They get written against a marked-up draft rather than anticipated. The plan provisions their home — `.claude/rules/writing.md`, path-scoped to `writing/**` so they load whenever a draft is touched — and seeds it with the two conventions already stated out loud (below), nothing more.

## Where the files live

**`writing/linkedin/` at the repo root.** Not `public/content/`, which is served publicly — unposted drafts crawlable before they run on LinkedIn is the wrong order of operations. Not `docs/`, whose every known subdirectory (`plans/`, `issue/`, `pr/`, `remove-before-merging/`) is a working artifact `/finalize` sweeps before merge, so a durable asset there is a trap set for the next agent pattern-matching on its neighbours. `writing/` costs one row in the layout table and generalises when the next channel arrives.

```
writing/
  linkedin/
    plan.md              # the backlog, the shapes, the form
    drafts/
      <slug>.md          # one file per post
```

Each draft carries frontmatter: `source` (the case-study section its facts come from), `shape`, `status` (`draft` | `approved` | `posted`), and `posted` once it is.

## Phase 1 — the PDF

The first post's attachment is the case study as a PDF. Attaching one on LinkedIn reads differently from linking: it opens in the feed, it flips locally, it does not ask the reader to leave. And the file earns its keep twice, since the site can offer the same download next to the markdown.

**`scripts/render-pdf.ts`, following the two render scripts already here.** `render-mermaid.ts` and `render-og.ts` established the shape — bare Node with type stripping, `findChromium()` from `scripts/lib/chromium.ts`, `execFileSync` against headless Chromium, a committed output, and a `--check` mode wired into `vet.sh` that recomputes a source hash and fails on drift. The PDF is `--print-to-pdf` with `--no-pdf-header-footer` instead of `--screenshot`, and `scripts/lib/` gains a small dependency-free static file server. Everything else is the pattern already in the tree.

Two things genuinely differ from its siblings and are the reason this is a phase rather than a footnote:

- **It renders the built page, not the markdown.** Mermaid and OG read source files; a PDF has to be the real compiled article, with its Shiki highlighting, its tables and its diagrams. So the script serves `out/` on a loopback port after `pnpm build` and prints from `http://localhost:<port>/…`, rather than `file://` — a static export references `/_next/…` absolutely, which a `file://` load cannot resolve. `--check` stays cheap and build-independent by hashing the markdown source and the print stylesheet, so vet's fan-out is unaffected.
- **A print stylesheet is most of the work.** Forcing the light theme (the page ships a light and a dark mermaid SVG per fence, and the dark one has no business in print), keeping code blocks from clipping at the page edge, breaking pages so a heading never orphans, resolving the video paragraph — which is a player on the web and nothing on paper — into a labelled link, and printing link targets that a PDF reader cannot hover. The CV page's existing print path is the precedent for where this CSS lives.

**Which documents get one:** the full `playgram.md` only, opted in by a `pdf: true` frontmatter flag rather than a slug hardcoded in the script — so the mini and nano cuts, and whatever comes next, are one line away without touching the renderer.

## Phase 2 — the calibration draft

The first post announces the case study and attaches the PDF. The operator supplied the opening almost whole, and **it is a voice specimen, not a first pass** — it does not get tightened, rebalanced, or improved. The two ellipses get filled and nothing else moves:

> So from March to August this year I was doing quite a fun thing -- well, fun if you're the weird kind who loves dabbling with parsings, abstractions and ... Once done, I thought, why not write a case study about it. I only went halfway and it's already a 40-minute read 🙈 Not really thinking anyone would seriously read it all -- but maybe skim through? There's some interesting stuff on ..., ..., and managing a team of 15+ agents at a time.

The three blanks are the only open drafting decisions, and each wants something concrete from the case study rather than a category:

- **the first** completes "parsings, abstractions and …" and should land on the strangest concrete object in the project — the 11.6 MB minified JSON that was the app's entire source, or the 3,487 files it got cut into;
- **the second and third** are the skim bait, and should name specifics that reward opening the file rather than topic labels: the lint rule whose failure mode was an authentication hole, the two type shapes that both compiled while logging zeroes for months, the migration numbered `0099` on a branch that had not merged `0098`.

No link, since the PDF is right there. No sign-off line.

**One caveat worth stating rather than discovering:** this post is an announcement, so the rules it calibrates will cover the personal register and say nothing about the teaching register the other eighteen posts live in. Post #3 below, the type-overlap gate, is the natural second calibration draft once the first round of rules exists — not in this change.

## Form

- **1,000 characters, hard ceiling.** Long enough for one idea completely; short enough to be read whole.
- **The first two lines carry it.** LinkedIn folds at roughly 200 characters, so the opening states the concrete thing rather than promising it. No "I want to share some thoughts on…", no question-as-opener.
- **`--`, never `—`.** Written as two hyphens on purpose: enough readers now treat an em dash as a machine's fingerprint that the correct punctuation has become the wrong signal. This is the first entry for `.claude/rules/writing.md`.
- **Emoji: sparingly, and only self-deprecating.** One 🙈 doing real work is in the voice; emoji as decoration, as bullet markers, or as enthusiasm is not. Text emoticons — `;-)` — are in the voice too.
- **One idea per post.** A second good idea is a second post.
- **No single-sentence-per-line staccato**, which is the house style of the genre and reads as the genre rather than as a person.
- **Numbers stay unrounded.** 3,487 files, 11.6 MB, 8,123 imports. The precision is the evidence.
- **English only.** The site is bilingual; this isn't.

## Linking back

Left open on purpose. The first post carries a PDF and needs no link at all, and whether later posts point anywhere is a question the first few answers will settle better than a rule written now.

## Sequencing

Free-standing and unnumbered — a numbered series makes every post after the first look like homework and makes a missed week visible. `plan.md` carries a suggested opening run, not a schedule: the announcement, then **1 → 3 → 12 → 5 → 2** below, which opens on the strangest artifact, follows with a bug that cost real money, then the handover — the one that most plainly answers "so did it work?" — before settling into practitioner material.

## The post inventory

Eighteen candidates, each anchored to something specific enough that it could not have been written by someone who wasn't there. Ordered by how well each stands alone, not by publishing order.

| # | Working title | What it actually teaches | Shape |
|---|---|---|---|
| 1 | Splitting by shape, not by size | An 11.6 MB minified JSON export cut into 3,487 files an agent can *navigate* instead of grep. Names derived from content, never position, so inserting a step doesn't rename its neighbours; long strings hoisted into `.txt` siblings so a prompt diffs as prose instead of one enormous escaped line. The point is the weekly re-export staying legible. | Artifact |
| 2 | The lint rule that closes an auth hole | Next turns every export from a `'use server'` file into an endpoint the browser can call — no route file, no ceremony. `safe-action-required` forces every one through a wrapper that establishes who is asking before the body runs. An agent will cheerfully ignore a paragraph in CLAUDE.md and will never once ship a lint error. | Footgun |
| 3 | Two types, one lie, months of zero analytics | `tokenCounts: { input, output }` beside DB columns named `inputTokens`/`outputTokens`. Both compiled. Every usage log recorded zero, found by accident months later. Then the second return nobody expects: about a third of reported overlaps are a *key that's lying*, not a missing base. | Footgun |
| 4 | A warning is a rule nobody enforces | 362 rules, every one explicitly `error` or `off`, nothing inherited from a recommended set a dependency bump can change under you. No rule is ever `warn`, because humans and LLMs alike treat a warning as negotiable and let it accumulate. | Reversal |
| 5 | One session, one thread | The 1M-token context window as the megapixel race — past a point more just means more noise on the matrix. The rule of thumb: past 200k you've strayed. Side thoughts get filed as issues, not appended to the conversation. | Reversal |
| 6 | The litmus test for a plan | If a plan is any good, a session with *none* of the conversation that produced it can implement it. If it can't, the plan was bad — a falsifiable test, and the reason plans belong in the repo rather than in a dialog box. | Reversal |
| 7 | Parallel agents meet sequential migration numbers | Migration `0099` generated on a branch that hadn't merged `0098`. Forked chain, dropped enum value, nothing failed at deploy for two whole migrations. Now a script walks the snapshots and refuses a broken chain. | Footgun |
| 8 | Rigid boundaries thicken the shared layer on their own | Between first production build and handover `src/` went 98,000 → 223,000 lines, and the layers that grew fastest *in relative terms* were the bottom ones: `shared` and `entities` nearly tripled while the app-specific top layer didn't quite double. Nobody planned that. | Measurement |
| 9 | Five agents told me it would be fine | Four models researching every decision independently, names stripped so the synthesis couldn't be biased, a fifth judging. The lone dissenter won both contested points. And the honest half: a good chunk of it was spreading the blame. Verdict 6.5/10. | Receipt |
| 10 | The plan listed a directory that never existed | The detailed migration plan's own "current state" inventory named an `entities/message` slice that was never in the repo, not once, in its entire history. After a while you're not reading the plan to find out what to do — you're reading it to find out how out of date it is. Verdict 6/10. | Receipt |
| 11 | What parallelism actually measures as | Across the one-day switch into the cloud: median unit of work 375 → 384 changed lines, units per day 6.2 → 8.2. Same-sized pieces, about a third more of them at a time. That's the number, and it is smaller than the marketing. | Measurement |
| 12 | The handover is the deliverable | Seventeen days after the last commit, 49 of 52 merged PRs were the team's — and the team is the Bubble developers who drew the app, two of whom opened their GitHub accounts during the project. What they shipped alone, listed plainly. | Measurement |
| 13 | The most popular Bubble plugin is a way out of Bubble | 538,000 installs for "run custom JavaScript"; two of the top five templates that year were homegrown application frameworks built on top of the platform. And step 0 of sending a chat message in this no-code app was a `Run javascript` action. | Reversal |
| 14 | The merge conflicts I was wrong about | The loudest fear about parallel agents, and after a thousand-plus merged PRs, zero problems — once one skill encoded the footguns. Agents resolve conflicts semantically, not just to a state where the file no longer has markers in it. | Receipt |
| 15 | Thirty-three files that describe how you work | Skills compose into a call graph because each is a file another can point at — `/implement` loads two by reference and hands to a third, which loads three more. And the half that's less fun: it's a second codebase, it drifts exactly like the first, and nothing lints it. | Receipt |
| 16 | Measure files in syntax nodes, not lines | The biggest files in the codebase are LLM prompts — hundreds of lines, as they should be, and structurally a single string. So the size report counts AST nodes, and deliberately never fails the build. A metric that fails is a metric people game. | Artifact |
| 17 | Fail slow, when the reader is an agent | Every check runs to completion even after one has failed, and the summary names all of them. Fail fast and the agent fixes one thing, pushes, and waits four minutes to be told about the next. Received wisdom, inverted for a reason. | Reversal |
| 18 | Reviewing the outcome, not the process | From three local agents whose *thinking* I read, to twenty in the cloud reviewed as pull requests. For 95% of cases the agent knows better; the remaining 5% is the entire argument for a knowledgeable human still being in the loop. | Receipt |

**Held in reserve rather than cut:** Part II of the case study is unwritten, and data migration, CI/CD economics, the 139-file navigation refactor, the home-grown XLS reader and the four-hour hang on malformed HTML are all posts. `plan.md` carries them as a "not yet" list so the backlog outlives the current material.

## The five shapes

Naming the shapes is what stops eighteen posts from being eighteen variations of one paragraph, and gives the future writing rules something concrete to constrain. The announcement post above belongs to none of them — a one-off does not need a category invented for it.

- **Footgun** — a specific bug, why it was invisible, and the thing that catches it now. Ends on the mechanism, never on a moral.
- **Measurement** — a number nobody else has, and what it does and doesn't mean. Must include the deflating half; a measurement that only flatters is an ad.
- **Reversal** — a received best practice that is wrong in a named context, with the reason it's usually right left standing.
- **Receipt** — an unflattering fact about my own work, scored. It does the most for credibility, and must never be false modesty about something that actually went well.
- **Artifact** — a small piece of real code, config or output, shown and then explained. The reader should be able to steal it.

Rough mix over any ten posts: 3 footgun, 2 measurement, 2 reversal, 2 receipt, 1 artifact. Receipts are the scarce ingredient — overused they turn into a bit.

## DRY notes

- **The case study is the single source of truth for every fact**, and posts have to restate those facts rather than reference them — a LinkedIn reader has nothing open. Duplication by necessity, so the mitigation is a pointer, not an extraction: each draft's `source` frontmatter names the case-study section it draws from, making "which posts repeat this number?" a grep instead of a memory. No script, no gate — a checker over eighteen prose files would cost more than the drift it prevents.
- **`render-pdf.ts` is a sibling of the two render scripts, not a generalisation of them.** The tempting extraction is a shared "render content with Chromium" module. It would be net-negative: `render-og.ts` rasterizes a standalone SVG at a fixed pixel size and `render-mermaid.ts` renders a fence in two themes, while this one serves a built site and prints paginated output. They share `findChromium()` — already extracted, already reused — and `contentHash`, and past those two the overlap is the word *Chromium*. What **is** shared and must not be re-derived: `scripts/lib/content-tree.ts` for walking the content tree, and the frontmatter-flag-plus-hash-manifest convention `og-renders.json` established, which the PDF manifest copies rather than reinvents.
- **The print stylesheet extends the CV's existing print path** rather than starting a second one; `@.claude/rules/styling.md` governs where it lands.
- **The five shapes are defined once, in `plan.md`.** Drafts name a shape in frontmatter and never restate its definition.
- **No post template, deliberately.** The obvious extraction — an opener slot, a body slot, a closer slot — is exactly the mechanism that produces the interchangeable voice this exercise exists to avoid. The five shapes are as much structure as is safe.
- **Nothing here touches `src/`** except the print styles, so no FSD or type-overlap surface is involved.

## Risks worth naming

- **The plan is only as good as the draft.** If the calibration draft comes back as slop, the inventory survives — the shapes and the facts are independent of it — but the writing rules become the load-bearing artifact and the backlog waits on them.
- **The PDF is the part that can go wrong quietly.** A print stylesheet fails by producing something ugly, not by failing a check, so the render has to be looked at before the post goes out. That is a QA checklist row, not something vet can hold.
- **Eighteen posts is a lot of surface for one case study.** Around post ten the material thins and the backlog needs Part II, or new work, to continue. Better named now than discovered in November.
